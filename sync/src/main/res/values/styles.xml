<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <!-- main theme colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>

    </style>

    <style name="AppTheme.Login" parent="Theme.AppCompat">

        <!-- main theme colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>

        <item name="android:background">@drawable/background_login</item>
        <item name="background">@drawable/background_login</item>
        <item name="android:colorButtonNormal">@color/background</item>
        <item name="android:actionBarStyle">@style/ActionBar.Login</item>
        <item name="actionBarStyle">@style/ActionBar.Login</item>
    </style>

    <style name="ActionBar.Login" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/primary_dark</item>
        <item name="background">@color/primary_dark</item>
    </style>

</resources>
